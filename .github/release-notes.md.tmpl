## Compatibility

Built against `zellij-tile {{ZELLIJ_TILE}}` — requires **Zellij >= {{ZELLIJ_TILE}}**.

## Changes
{{CHANGES}}

## Install

```sh
curl -L -o ~/.config/zellij/plugins/zellij-tab-namer.wasm \
  {{REPO_URL}}/releases/download/{{TAG}}/zellij-tab-namer.wasm
```

Load it in `~/.config/zellij/config.kdl` (see the README).
