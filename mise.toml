# Dev tooling pinned for this repo, see https://mise.jdx.dev
# Bootstrap with `mise install`.
#
# git-cliff generates CHANGELOG.md from the conventional commits (see cliff.toml).
# Pinning it keeps the generated changelog identical whoever runs `task changelog`.
[tools]
git-cliff = "2.13.1"
